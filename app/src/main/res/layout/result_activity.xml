<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:gravity="center">

    <TextView
        android:id="@+id/resul_score"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="[SCORE]" />

    <Button
        android:id="@+id/result_tweet_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Partager mon score sur Twitter"
        android:layout_below="@id/resul_score"/>

    <Button
        android:id="@+id/result_btn_partager"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Partager mon score"
        android:layout_below="@id/result_tweet_btn"/>

</RelativeLayout>